spring:
  cloud:
    azure:
      active-directory:
        enabled: true
        profile:
          tenant-id: <>
        credential:
          client-id: <>
          client-secret: <>

server:
  port: 8080
  forward-headers-strategy: native